#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
    int n,q,a[10010];
    cin >> n >> q;
    for (int i=1; i<=n; i++) cin >> a[i];
    sort(a+1,a+n+1);
    int l,r,tong=0;
    for (int i=1; i<=q; i++)
    {
        cin >> l >> r;
        for (int i=r)
        //Nhầm :))
    }
}
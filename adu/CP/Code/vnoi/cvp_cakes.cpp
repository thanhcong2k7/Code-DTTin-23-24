//User NgTCg007
#include <bits/stdc++.h>
using namespace std;
const long long maxN = 1e5+5;
int s,n,a[maxN];
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    cin >> s >> n;
    for (int i=0; i<n; i++)
        cin >> a[i];
    sort(a,a+n);
    for (int i=0; i<n; i++)
        
    return 0;
}
#include <bits/stdc++.h>
using namespace std;
int main(){
	freopen("Mices.inp","r",stdin);
	freopen("Mices.out","w",stdout);
	ios_base::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
	int t,n;
	cin >> t;
	while(t--){
		cin >> n;
		int a[n],b[n];
		for (int i=0; i<n; i++)
			cin >> a[i];
		for (int i=0; i<n; i++)
			cin >> b[i];
	}
	return 0;
}
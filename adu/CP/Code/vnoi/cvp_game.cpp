//User NgTCg007
#include <bits/stdc++.h>
const long long maxN = 1e5+5;
int n,b[maxN],c[maxN];
int bs(int k){
	int mid=(hi-lo)/2,hi=n,lo=1;
	while(b[mid]>=k){}
}
using namespace std;
int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);cout.tie(0);
	for (int i=1; i<=n; i++)
		cin >> b[i];
	for (int i=1; i<=n; i++)
		cin >> c[i];
	sort(b+1,b+n+1);
	int m;
	for (int i=1; i<=n; i++){
		int k=-c[i];
	}
	return 0;
}
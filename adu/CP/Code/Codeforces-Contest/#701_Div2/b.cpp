#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,q,k,a[100002]={0},l,r;
    cin >> n >> q >> k;
    for (int i=1; i<=n; i++)
        cin >> a[i];
    for (int i=1; i<=q; i++){
        cin >> l >> r;
    }
}